name: music_player
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  audio_service:
  flutter:
    sdk: flutter
  get:
  on_audio_query:
  transparent_image:
  just_audio:
  permission_handler:
  audio_video_progress_bar: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/playlist.jpg

